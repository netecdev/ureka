query GetApplication($id: ID!) {
    application(id: $id) {
        annotations {
            x
            y
            width
            height
            description
            type
        }
    }
}
