query GetApplication($project: ID!, $id: ID!) {
    application(project: $project id: $id) {
        annotations {
            id
            x
            y
            width
            height
            description
            type
        }
    }
}
